<!DOCTYPE html>
<html>
    <head>
        <title>Outreach</title>
        <meta name="description" content="The best way to keep track of outreach to mentors, companies, recruiters, and customers alike.">
        <meta name="keywords" content="outreach,cameron,cabo,cameroncabo,email,linkedin,mentor,contact,contacts,long-term">
        <meta charset="UTF-8">
        <meta name="author" content="Cameron Cabo">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta property="og:title" content="Outreach">
        <meta property="og:description" content="outreach,cameron,cabo,cameroncabo,email,linkedin,mentor,contact,contacts,long-term">
        <meta property="og:type" content="website">
        <meta property="og:url" content="https://outreachly.herokuapp.com">

        <%= csrf_meta_tags %>

        <link href="https://fonts.googleapis.com/css?family=Varela+Round" rel="stylesheet">
        <script src="https://use.fontawesome.com/297cffcc6b.js"></script>
        <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
        <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
        <link rel="manifest" href="/manifest.json">
        <meta name="msapplication-TileColor" content="#ffffff">
        <meta name="theme-color" content="#ffffff">

        <%= favicon_link_tag 'favicon.ico' %>
    </head>

    <body>
        <%= render 'layouts/header' %>

        <div class="container display-none" id="alerts">
            <div class="row">
                <div class="col-12 col-md-8 offset-md-2 col-lg-6 offset-lg-3">
                    <% flash.each do |name, msg| %>
                        <%= content_tag :div, msg, class: [:alert, alert_for(name)]%>
                    <% end %>
                </div>
            </div>
        </div>


        <%= yield %>


        <%= render 'layouts/footer' %>

        <script>
            $(document).ready(function() {
                $('#alerts').slideDown(250).delay(1500).slideUp(250);
            });

            // function resizeBacker() {
            //     var $backer = $('.navbar-backer');
            //     $backer.height($('.navbar').outerHeight());
            //     $backer.width('112px');
            //     $('.navbar-backer-skew').width('132px');
            // }
            //
            // $(document).ready(function() {
            //     resizeBacker();
            // });
            //
            // $(window).resize(function() {
            //     resizeBacker();
            // });

            $('.collapse-trigger').click(function() {
                $('.collapse-trigger i.absolute-right').toggleClass('rotate');
            });
        </script>
    </body>
</html>
